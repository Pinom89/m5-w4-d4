import { createContext } from "react";

// creo una costante che andrà a racchiudere tutti i componenti ai quali voglio che venga applicato l'hook createContext
export const ThemeContext = createContext();
 // default export ThemeContext=>posso non inserire default se  ci sono più context nello stesso file oppure prima del const posso inserire direttamente export


 

